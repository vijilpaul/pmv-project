import { ApiInterceptorInterceptor } from './api-interceptor-interceptor';

describe('ApiInterceptorInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApiInterceptorInterceptor()).toBeTruthy();
  });
});
